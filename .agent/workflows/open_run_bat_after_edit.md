---
description: Ensure run.bat is opened after any code modification
---

After you utilize any tool to modify code, add new functions, or fix bugs in the project (especially in `gui.py` or `browser_bot.py`), you must:

1.  Open the `run.bat` file using the `view_file` tool.
    - Path: `c:/Users/cheet/OneDrive/Documents/GitHub/ddcm-browser-helper/run.bat`
    
This serves as a confirmation step and ensures the entry point of the application is always visible or ready to be checked by the user.
